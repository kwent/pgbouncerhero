.ui.active.segment

  %h4.ui.header
    .content Clients

  %table.ui.compact.table
    %thead
      %tr
        - clients.first.keys.each do |key|
          %th= key.titleize
    %tbody
      - clients.each do |row|
        %tr
          - row.each do |k,v|
            %td= v
