.ui.active.segment

  %h4.ui.header
    .content Confs

  %table.ui.compact.table
    %thead
      %tr
        - conf.first.keys.each do |key|
          %th= key.titleize
    %tbody
      - conf.each do |row|
        %tr
          - row.each do |k,v|
            %td= v
